[setting.snx_package]
defaultValue = "synthetix:3.0.4-alpha.0"

[setting.owner]

[setting.salt]
defaultValue = "snax"

[provision.system]
source = "<%= settings.snx_package %>"
options.owner = "<%= settings.owner %>"
options.salt = "<%= settings.salt %>"
