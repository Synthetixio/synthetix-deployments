[setting.snx_package]
defaultValue = "synthetix:3.1.1"

[setting.owner]

[setting.targetPreset]

[setting.salt]
defaultValue = "snax"

[setting.bundleSalt]
defaultValue = "snax"

[provision.system]
source = "<%= settings.snx_package %>"
targetPreset = "<%= settings.targetPreset %>"
options.owner = "<%= settings.owner %>"
options.salt = "<%= settings.salt %>"
options.bundleSalt = "<%= settings.bundleSalt %>"
